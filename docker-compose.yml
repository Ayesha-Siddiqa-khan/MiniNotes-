services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - FLASK_DEBUG=0
      - DB_PATH=/app/data/notes.db
    volumes:
      - notes-data:/app/data

volumes:
  notes-data:
